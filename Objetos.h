#ifndef OBJETOS_H_INCLUDED
#define OBJETOS_H_INCLUDED
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/*
typedef struct Objeto
{
    char prefacio[100];///Nombre del objeto, mob, etc.
    int mod;///Modificadores de daño-curacion-defensa, si el objeto los tiene
    int tipo;///0. Pergamino, 1. Pocion,2. Arma,3. Armadura,-1. Inventario Vacio
}Objeto;

recojerLoot(jugador heroe, objeto inventario[])
fopen(PyP)
fread(objeto aux);
if(inventario)
    while(inventario[i].tipo!=-1)
{
    i++;
}
if(i<10)
{
    hay espacio
}
else
{
    no espacio
}
srand(time(NULL))
rand()%2
if(rand==0)
{
    abre armas
}
else
{
    abre armor
    heroe.armadura=aux.modificador
}

1
1 1*mob fseek(mob,SEEK_SET,cantcombates*mob)
1 2*mob*/
#endif // OBJETOS_H_INCLUDED
